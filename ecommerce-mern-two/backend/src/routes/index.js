const { ComentsRoutes } = require('./Coments.Routes');
const { FavoriteRoutes } = require('./Favorites.Routes');
const { LikesRoutes } = require('./Likes.Routes');
const { UserRoutes } = require('./User.Routes');

module.exports = {
    ComentsRoutes,
    FavoriteRoutes,
    LikesRoutes,
    UserRoutes,
};