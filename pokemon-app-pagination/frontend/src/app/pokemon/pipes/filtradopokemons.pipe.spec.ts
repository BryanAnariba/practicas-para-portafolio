import { FiltradopokemonsPipe } from './filtradopokemons.pipe';

describe('FiltradopokemonsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradopokemonsPipe();
    expect(pipe).toBeTruthy();
  });
});
